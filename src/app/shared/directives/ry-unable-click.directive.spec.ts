import { RyUnableClickDirective } from './ry-unable-click.directive';

describe('RyUnableClickDirective', () => {
  it('should create an instance', () => {
    const directive = new RyUnableClickDirective();
    expect(directive).toBeTruthy();
  });
});
