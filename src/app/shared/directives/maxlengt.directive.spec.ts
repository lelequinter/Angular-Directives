import { MaxlengtDirective } from './maxlengt.directive';

describe('MaxlengtDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxlengtDirective();
    expect(directive).toBeTruthy();
  });
});
